apiVersion: v2
name: airllm
description: AirLLM — self-hosted, OIDC-governed LLM gateway with DLP, on Kubernetes
type: application
version: 0.1.0
appVersion: "0.1.0"
home: https://github.com/ipsupport-llc/ipsupport-airllm
sources:
  - https://github.com/ipsupport-llc/ipsupport-airllm
kubeVersion: ">=1.23.0-0"
# No required dependencies: Postgres/Redis are external (referenced via existingSecret),
# and KEDA/Prometheus-operator CRDs are optional (gated by values). `helm template`
# renders with zero network access.
